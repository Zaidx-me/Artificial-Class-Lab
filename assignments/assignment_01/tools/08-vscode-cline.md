# 08 — VS Code + Cline

**Type:** Editor + Autonomous Agent extension · **Developer:** saoudrizwan (OSS)

## Install
```bash
code --install-extension saoudrizwan.claude-dev
```
AUR/Open VSX ID: `saoudrizwan.claude-dev` (6M+ downloads on Open VSX).

## Setup
- Provide an API key in Cline settings (Claude, OpenAI, Gemini, etc.)
- Autonomous agent: creates/edits files, runs commands, browses — with per-step permission

## Notes from our install
- On Open VSX the key exists as `saoudrizwan/claude-dev`; from the Microsoft marketplace
  (after product.json re-point) the same ID installed.
- Free extension; you pay only your API token costs.